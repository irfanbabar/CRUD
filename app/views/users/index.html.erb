<h1>Users</h1>
<table>
	<tr>
		<th>Name</th>
		<th>Age</th>
		<th>Actions</th>		
	</tr>
	
	<% @users.each do |user| %>
		<tr>
			<td><%= user.name %></td>
			<td><%= user.age %></td>
			<td>
				<%= link_to 'Show', "/users/#{user.id}/show" %>			
			</td>
		</tr>
	<% end %>
	
</table>